# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
# Cloudflare Pages Functions local secrets (wrangler) — must never be committed
.dev.vars
.dev.vars.*
# Stray credential dumps — never commit a token/secret file
*token*.txt
*.secret

# Local tooling (Claude Code preview config, etc.)
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
