# Run lint-staged (eslint --fix + prettier --write on staged .ts/.vue files)
npx lint-staged

# Typecheck — reject commit if there are TypeScript / vue-tsc errors
npm run typecheck

# Release gate — verify version, CHANGELOG entry, and git tag alignment
npm run release-gate
